fastapi>=0.115.6,<0.120.0
uvicorn[standard]==0.32.0
httpx==0.27.2
# Optional SDKs — uncomment if your app uses them:
# google-cloud-firestore==2.18.0    # for sandbox_db
# (notion_db uses httpx, no extra dep needed)
